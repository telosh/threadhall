# Contributing

1. Node.js 22+ を使用（`.nvmrc` 参照）
2. `cp .env.example .env.local` のうえ、`docker compose up sqld` で libSQL を起動
3. `npm run lint` / `npm run typecheck` / `npm run build` が通ること

Pull Request では変更理由と影響範囲を短く記載してください。
