# 実行結果スクリーンショット

開発サーバー起動後、Chrome DevTools MCP で取得。

| ファイル | 内容 |
|----------|------|
| `home-organizations-1.png` | 組織1件作成後のトップ（`organizations` 件数・フォーム） |
| `home-after-org-create.png` | 同系（リロード前後のいずれかの取得） |

再取得手順: `npm run dev` → `http://127.0.0.1:3000/` を開く。
